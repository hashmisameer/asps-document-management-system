/**
 * What the application calls itself, in one place.
 *
 * These strings appear on the sign-in page, the registration page and the
 * header of every signed-in screen. They used to be typed out separately in
 * each, which meant renaming the company changed one screen and quietly left
 * the others saying something else - the sort of difference nobody notices
 * until a person outside the team sees two names for the same system.
 *
 * Change them here and every screen follows.
 */

/** The company, above the application name. */
export const ORGANISATION_NAME = 'ASPS International LLP'

/** The application itself. */
export const APP_NAME = 'DMS'
